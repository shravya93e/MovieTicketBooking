# Application Name
spring.application.name=UserService

# These properties are managed in the GitHub repository to maintain centralized configuration.

# Hibernate Properties
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# MySQL Properties
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.username=root


# Database URL
spring.datasource.url=jdbc:mysql://localhost:3306/userservice?createDatabaseIfNotExist=true

# Server Port
server.port=8011

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# Configuration Server Import
spring.config.import=optional:configserver:http://localhost:2424
